name: Windows W2 Settings Audit

on:
  push:
    branches: [audit/windows-w2-settings]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  windows_w2:
    name: Windows W2 Settings Audit
    runs-on: windows-latest
    timeout-minutes: 25

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install app dependencies
        run: npm install --no-audit --no-fund

      - name: Install pinned Playwright Electron driver
        run: npm install --no-save --no-package-lock @playwright/test@1.63.0

      - name: Build auth-independent Windows renderer
        run: npm run build:web
        env:
          VITE_VISUAL_TEST: '1'

      - name: Run Windows W2 Settings audit
        continue-on-error: true
        run: npx playwright test tests/audit/windows-w2-settings.spec.mjs --workers=1 --reporter=list

      - name: Upload Windows W2 screenshots and results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: free-ai-windows-w2-audit
          path: artifacts/windows-w2/**
          if-no-files-found: error

      - name: Fail if W2 contains real UI failures
        if: always()
        shell: pwsh
        run: |
          $file = 'artifacts/windows-w2/results.json'
          if (-not (Test-Path $file)) { throw 'Windows W2 result file was not produced.' }
          $rows = Get-Content $file -Raw | ConvertFrom-Json
          $failed = @($rows | Where-Object { $_.status -eq 'FAIL' })
          if ($failed.Count -gt 0) {
            $failed | ConvertTo-Json -Depth 6 | Write-Host
            throw "Windows W2 found $($failed.Count) real UI failure(s)."
          }
          Write-Host 'Windows W2 audit finished without real UI failures. BLOCKED items remain explicitly reported.'
